<%= simple_form_for(@product, class: "max-width-600") do |f| %>
  <%= f.error_notification %>

  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :price %>
  <%= f.input :permalink %>

  <%= f.button(:submit, class: "ui primary button") %>
<% end %>
